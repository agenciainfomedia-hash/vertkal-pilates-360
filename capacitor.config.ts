import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vertkalpilates360',
  appName: 'vertkal-pilates-360',
  webDir: 'dist'
};

export default config;